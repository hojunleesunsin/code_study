조합(Combination)이란?
조합은 nCr로 표현하고, 이는 n개의 숫자에서 r개를 뽑는 경우의 수를 뜻한다.
코딩 테스트에 자주 출제되는 문제이며 동적 계획법을 이해하는데 기초가 되므로 중요하다.

순열이란?
조합과 비교되는 순열은 nPr로 표현되고, n개의 숫자 중 r개를 뽑는 순서를
고려해 나열할 경우의 수를 이야기한다.

조합과 순열의 차이 == 순서의 고려 유무
예) 조합에서는 데이터 1, 2, 3과 3, 2, 1을 같은 경우로 판단하고, 순열은 다른경우로 판단

조합 점화식
=>      D[i][j] = D[i-1][j] + D[i-1][j-1]
